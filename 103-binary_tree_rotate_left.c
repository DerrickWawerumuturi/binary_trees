#include "binary_trees.h"

binary_tree_t *binary_tree_rotate_left(binary_tree_t *tree) {
    binary_tree_t *temp, *orig, *left;

    if (tree ==  NULL || tree->right == NULL)
        return (NULL);

    
    
}